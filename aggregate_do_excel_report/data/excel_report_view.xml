<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data>

        <!--<record model="ir.actions.server" id="generate_aggregate_excel_action">
            <field name="name">Order Release Report</field>
            <field name="model_id" ref="x_aggregate_delivery_order"/>
            <field name="state">code</field>
            <field name="code">
                order_release_object = env['do.release']
                for record in records:
                    action = order_release_object.generate_excel_report(record)
            </field>
        </record>-->

    </data>
</odoo>