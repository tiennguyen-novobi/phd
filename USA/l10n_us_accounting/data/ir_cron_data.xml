<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="ir_cron_account_invoice_aging_days" model="ir.cron">
        <field name="name">Account Invoice: Aging Days Auto Update</field>
        <field name="active" eval="True"/>
        <field name="user_id" ref="base.user_root"/>
        <field name="model_id" ref="account.model_account_move"/>
        <field name="state">code</field>
        <field name="code">model._cron_aging_days()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <!--keep for later use-->
        <!--<field name="nextcall"-->
               <!--eval="datetime(datetime.now().year, datetime.now().month, datetime.now().day) + timedelta(days=1)"/>-->
    </record>
</odoo>