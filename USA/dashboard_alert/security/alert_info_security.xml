<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record model="ir.rule" id="alert_info_rule">
            <field name="name">Personal Alert Information</field>
            <field name="model_id" ref="model_alert_info"/>
            <field name="global" eval="True"/>
            <field name="domain_force">['|',('recipient_ids.id', '=', user.id), ('creator_id', '=', user.id),
                '|',('company_id','=',False),('company_id','=',user.company_id.id)]</field>
        </record>

    </data>
</odoo>
